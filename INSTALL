$Id$

This application requires a full Debian package build - it cannot be installed using only
the setup.py script:

dpkg-buildpackage -us -uc
cd ../
For GNOME: sudo dpkg -i autokey-gtk_<version>.deb autokey-common_<version>.deb
For KDE: sudo dpkg -i autokey-qt_<version>.deb autokey-common_<version>.deb